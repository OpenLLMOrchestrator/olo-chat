# Backend URL so the app calls port 7080 directly (Request URL will show localhost:7080)
VITE_API_BASE=http://localhost:7080

# WebSocket ping interval and reconnect delay in seconds (default 10). Same value used for both.
# VITE_WS_PING_INTERVAL_SEC=10
